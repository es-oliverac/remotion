services:
  remotion-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: remotion-fastapi
    ports:
      - "8000:8000"
    volumes:
      - remotion-outputs:/app/outputs
    environment:
      - APP_NAME=Remotion FastAPI Server
      - DEBUG=false
      - MAX_CONCURRENT_RENDERS=2
      - OUTPUT_DIR=/app/outputs
      - BASE_URL=https://n8n-remotion.alzadl.easypanel.host
      - MAX_BROWSER_INSTANCES=3
      - MAX_BROWSER_IDLE_SECONDS=300
      - JOB_CLEANUP_HOURS=24
      - NODE_PATH=/usr/bin/node
      # Frontend URL for Docker container communication
      - REMOTION_FRONTEND_URL=http://remotion-frontend:3000
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G

volumes:
  remotion-outputs:
